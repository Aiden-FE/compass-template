# {{projectName}}

> {{projectDescription}}

## Getting Started

`npm install -g {{projectName}}` 全局安装cli 或着 npm使用 `npx {{projectName}} <command>`, pnpm使用`pnpm dlx {{projectName}} <command>`等方式临时使用

`{{commandName}} --help` 获取帮助信息

`{{commandName}} update` Cli更新检查

## Contributes

### Install

`npm install`

### Base commands

- `npm run dev`    启用开发模式,可通过 `node index.js` 执行命令
- `npm run build`  生产构建
